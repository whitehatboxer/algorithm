package main

// 使用 go 的话，感觉没法处理第一个参数，也没法使用递归

func genNewMap(_map map[string]interface{}, layers []string, value map[string]string) {
	if _map == nil {
		return
	}

	if len(layers) == 0 {

	}
}

func main() {}
